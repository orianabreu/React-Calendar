import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    calendar: {
      height: '80vh',
      width: '80vw',
      marginLeft: '10vw',
    //   marginTop: '10vh'
    }
  }));

export default useStyles;